<div class="flex flex-col items-center justify-center max-w-2xl py-32 m-auto text-center">
  <h1 class="mb-2">Welcome to Jumpstart</h1>
  <p class="mb-6 text-xl leading-normal text-gray-700">Jumpstart is the perfect starting point for your next Ruby on Rails app. Skip the boilerplate setup and build your app faster.</p>

  <% if Rails.env.development? %>
    <div class="flex flex-col items-center md:flex-row">
      <div class="w-full md:w-auto">
        <%= link_to "Configure Jumpstart", jumpstart_path, data: { turbo: false }, class: "btn btn-primary lg:mr-4 mr-0 lg:mb-0 mb-4" %>
      </div>

      <div>
        <span class="hidden h-10 mr-4 border border-gray-200 md:inline"></span>
        <%= link_to "Create a User", new_user_registration_path, class: "btn btn-tertiary btn-outline lg:mr-4 mr-0 lg:mb-0 mb-4" %>
        <%= link_to "Read the Docs", jumpstart.docs_path, target: :_blank, data: { turbo: false }, class: "btn btn-tertiary btn-outline lg:mr-4 mr-0 lg:mb-0 mb-4" %>
      </div>
    </div>
  <% end %>
</div>
