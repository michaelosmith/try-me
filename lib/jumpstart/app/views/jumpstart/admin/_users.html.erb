<h3>Create an Admin User</h3>
<p>Admin users have access to the admin dashboard. They're marked in the database with the <code>admin</code> flag set to true.</p>

<table class="table my-6 text-left table-bordered">
  <thead>
    <tr>
      <th class="px-3 py-2 text-sm font-bold text-gray-600 bg-gray-100 border-b border-gray-200">Name</th>
      <th class="px-3 py-2 text-sm font-bold text-gray-600 bg-gray-100 border-b border-gray-200">Email</th>
    </tr>
  </thead>
  <tbody id="admin_users">
    <%= render partial: "jumpstart/users/user", collection: User.where(admin: true).order(created_at: :asc), as: :user %>
  </tbody>
</table>

<a class="cursor-pointer btn btn-primary btn-outline" onclick="Rails.fire(document.querySelector('#show_admin_user_form'), 'click')">Create Admin User</a>
